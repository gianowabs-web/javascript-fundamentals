/*
Number data type is used to represent numbers 
-examples
  integers
  decimals
  exponentials
  octal
  hexadecimal
  binary

*/

let integernumber = 100; // these are whle numbers without decimal.
let decimalnumbers = 78.45; // numbers with decimals
let exponentialnumber = 1.4e154;
let octalnumber = 0o10; // decimal version would be 8
let hexadecimalnumber =0x3E8;// decimal version would be 1000
let binarynumber = 0b101; //decimal version would be 5

console.log(integernumber, decimalnumbers, exponentialnumber, octalnumber);
console.log(hexadecimalnumber, binarynumber);