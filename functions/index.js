let maths= Number(prompt("Enter marks for maths:"));
let Eng=Number(prompt("Enter marks for english"));
let Kis =Number(prompt("Enter marks for Kiswahili"));

// calculate the total marks
let total_marks = maths + Eng + Kis;
document.writeln(total_marks);